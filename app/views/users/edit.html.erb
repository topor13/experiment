<%= content_tag :h1, t('.editing_user') %>

<%= render 'form' %>

<%= link_to t('.back'), posts_path %>
<%= link_to t('.users_list'), users_path if current_user && current_user.roles.where(name: 'admin').exists? %>